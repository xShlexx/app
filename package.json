{
  "name": "mi-app-android",
  "version": "1.0.0",
  "description": "Empaquetado de sitio web como app Android con Capacitor",
  "scripts": {
    "build:apk:debug": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:apk:release": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/assets": "^3.0.5"
  }
}
